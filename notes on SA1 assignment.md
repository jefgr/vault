Hints for the problem descriptions
1) use the ids
2) use a fold for this
3) just a ratio of open/open+closed
4)  
5) for each hour report average wait time
6) fast pass for only one attraction, relative to the average wait time at a certain hour

.groupby function for substreams